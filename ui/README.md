# Nova Document Pipeline UI

React + Vite UI for running one trade document through the Nova pipeline and querying stored runs.

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL=http://localhost:8000` if your API is not on the default URL.
